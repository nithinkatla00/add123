var router;

router = require('./router');

module.exports = function(store, actionType, actionData) {
  switch (actionType) {
    case 'router/go':
      return router.go(store, actionData);
    default:
      console.warn(":Unknown action type: " + actionType);
      return store;
  }
};
